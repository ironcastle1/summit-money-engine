MERLIN CNC V3 VERIFICATION

Verified in build environment:
- 25 JavaScript files pass `node --check` / preflight syntax verification.
- Static HTML/JS selector audit found no missing static IDs; remaining selector names are generated dynamically inside product/inventory dialogs.
- V3 database migration is additive and preserves existing V1/V2 tables and records.
- Dashboard layout persistence table is created automatically.
- OPENAI_API_KEY is intentionally not embedded in the code.

Dependency-backed tests:
- `npm install` could not complete in the build environment before the execution timeout.
- Consequently tests requiring dxf-parser and better-sqlite3 could not run here.
- The existing test suite and GitHub CI remain included and should run after dependencies install on GitHub/Render/local Node 20.

Important live requirements:
- Render persistent disk must remain mounted at /var/data for durable SQLite/DXF storage.
- OPENAI_API_KEY must be configured on the Render backend for Ask MERLIN and automatic market research.
