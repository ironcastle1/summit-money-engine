import { addHours, expired } from './time.js';
const policy = Object.freeze({ SEV1: { acknowledgeHours: 0.25, responseHours: 0.5, resolveHours: 4 }, SEV2: { acknowledgeHours: 1, responseHours: 2, resolveHours: 12 }, SEV3: { acknowledgeHours: 4, responseHours: 8, resolveHours: 48 }, SEV4: { acknowledgeHours: 12, responseHours: 24, resolveHours: 120 } });
export function supportSla(caseRecord, now = Date.now(), customPolicy = {}) { const rule = { ...(policy[caseRecord.severity] || policy.SEV3), ...(customPolicy[caseRecord.severity] || {}) }; const created = caseRecord.createdAt; const deadlines = { acknowledgeAt: addHours(created, rule.acknowledgeHours).toISOString(), responseAt: addHours(created, rule.responseHours).toISOString(), resolveAt: addHours(created, rule.resolveHours).toISOString() }; return Object.freeze({ severity: caseRecord.severity, rule: Object.freeze(rule), deadlines: Object.freeze(deadlines), breaches: Object.freeze({ acknowledgement: !caseRecord.acknowledgedAt && expired(deadlines.acknowledgeAt, now), response: !caseRecord.firstResponseAt && expired(deadlines.responseAt, now), resolution: !caseRecord.resolvedAt && expired(deadlines.resolveAt, now) }) }); }
