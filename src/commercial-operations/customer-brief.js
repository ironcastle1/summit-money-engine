export function customerBrief(input = {}) { return Object.freeze({ tenantId: input.tenant?.id, title: `${input.tenant?.name || 'Customer'} account brief`, state: input.tenant?.state || 'UNKNOWN', planId: input.tenant?.planId || 'FREE', segment: input.tenant?.segment || 'SELF_SERVE', seats: input.seats || 0, activeSeats: input.activeSeats || 0, onboarding: input.onboarding || null, health: input.health || null, retention: input.retention || null, expansion: input.expansion || null, openSupportCases: input.openSupportCases || 0, openIncidents: input.openIncidents || 0, nextActions: Object.freeze(input.nextActions || []), generatedAt: new Date().toISOString() }); }
