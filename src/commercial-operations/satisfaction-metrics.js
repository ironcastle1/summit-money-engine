import { average } from './utilities.js';
export function satisfactionMetrics(feedback = []) { const nps = feedback.filter(item => item.type === 'NPS' && item.score !== null); const promoters = nps.filter(item => item.score >= 9).length; const detractors = nps.filter(item => item.score <= 6).length; const npsScore = nps.length ? Math.round((promoters - detractors) / nps.length * 100) : null; const csat = feedback.filter(item => item.type === 'CSAT' && item.score !== null); const ces = feedback.filter(item => item.type === 'CES' && item.score !== null); return Object.freeze({ responses: feedback.length, nps: npsScore, promoters, detractors, csat: csat.length ? Math.round(average(csat.map(item => item.score)) * 100) / 100 : null, ces: ces.length ? Math.round(average(ces.map(item => item.score)) * 100) / 100 : null, ideas: feedback.filter(item => item.type === 'IDEA').length, bugs: feedback.filter(item => item.type === 'BUG').length }); }
