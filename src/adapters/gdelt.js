import {fetchResponse} from '../core/http.js';import {clean,parseDate} from '../core/utils.js';
export async function runGdelt(source){const q=encodeURIComponent(source.query);const url=`https://api.gdeltproject.org/api/v2/doc/doc?query=${q}&mode=ArtList&maxrecords=75&format=json&sort=HybridRel&timespan=72h`;const r=await fetchResponse(url);let d;try{d=JSON.parse(r.body);}catch{throw new Error('GDELT invalid JSON');}const articles=d.articles||[];return{records:articles.map(a=>({title:clean(a.title),summary:clean(a.seendate?`Reported ${a.seendate}. ${a.domain||''}`:''),url:a.url,publishedAt:parseDate(a.seendate),sourceName:a.domain||source.name,sourceDomain:a.domain||'',sourceId:source.id,sourceRegion:source.region,language:a.language||'',image:a.socialimage||''})).filter(x=>x.title&&x.url).slice(0,75)};}
