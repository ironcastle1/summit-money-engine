export async function automationDiagnostics(platform, owner = 'anonymous') { const [workflows, runs, rules, notifications, audit] = await Promise.all([platform.workflows.summary(owner), platform.runs.summary(owner), platform.rules.list(owner).then(items => ({ total: items.length, enabled: items.filter(item => item.enabled).length })), platform.notifications.summary(owner), platform.audit.verify(owner)]); return Object.freeze({ status: audit.valid ? 'READY' : 'DEGRADED', workflows, runs, rules, notifications, audit, scheduler: platform.scheduler.status(), actions: platform.actions.list(), metrics: platform.metrics.snapshot(), generatedAt: new Date().toISOString() }); }
