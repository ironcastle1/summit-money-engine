import { clamp, finite, percentile } from './utilities.js';
export function capacityModel(input = {}) { const demand = (input.demandSamples || []).map(Number).filter(Number.isFinite); const peak = percentile(demand, 99) ?? finite(input.currentDemand); const currentCapacity = Math.max(1, finite(input.currentCapacity, 1)); const headroom = clamp((currentCapacity - peak) / currentCapacity * 100, -1000, 100); const forecast = peak * (1 + finite(input.growthPercent, 0) / 100); const required = Math.ceil(forecast / Math.max(0.05, finite(input.targetUtilization, 70) / 100)); return Object.freeze({ peakDemand: peak, currentCapacity, headroomPercent: headroom, forecastDemand: forecast, requiredCapacity: required, additionalCapacity: Math.max(0, required - currentCapacity), state: headroom < 0 ? 'EXHAUSTED' : headroom < 15 ? 'TIGHT' : headroom < 30 ? 'WATCH' : 'HEALTHY' }); }
