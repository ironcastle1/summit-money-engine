import { finite, ratio } from './utilities.js';
export function burnRate(slo, recent = [], baseline = []) { const failed = rows => rows.reduce((sum, item) => sum + (item.good !== null && item.total !== null ? Math.max(0, finite(item.total) - finite(item.good)) : 0), 0); const total = rows => rows.reduce((sum, item) => sum + (item.total !== null ? finite(item.total) : 0), 0); const allowed = Math.max(0.000001, (100 - finite(slo?.target, 99.9)) / 100); const recentRate = total(recent) ? ratio(failed(recent), total(recent)) : 0; const baselineRate = total(baseline) ? ratio(failed(baseline), total(baseline)) : recentRate; const fast = recentRate / allowed; const slow = baselineRate / allowed; return Object.freeze({ fast, slow, state: fast >= 14 && slow >= 6 ? 'CRITICAL' : fast >= 6 || slow >= 3 ? 'HIGH' : fast >= 2 ? 'ELEVATED' : 'NORMAL', page: fast >= 14 && slow >= 6, recentFailureRate: recentRate, baselineFailureRate: baselineRate }); }
