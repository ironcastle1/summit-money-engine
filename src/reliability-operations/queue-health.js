import { clamp, ratio } from './utilities.js';
export function queueHealth(queue) { const net = Number(queue.ingressPerMinute || 0) - Number(queue.egressPerMinute || 0); const drainMinutes = queue.egressPerMinute > queue.ingressPerMinute ? queue.depth / Math.max(0.001, queue.egressPerMinute - queue.ingressPerMinute) : null; const pressure = clamp(ratio(queue.depth, Math.max(1, queue.consumers * 1000)) * 100 + Math.min(50, queue.oldestJobAgeSeconds / 60)); const state = queue.deadLetters > 100 || queue.oldestJobAgeSeconds > 3600 ? 'CRITICAL' : pressure >= 75 || net > 100 ? 'HIGH' : pressure >= 40 || net > 0 ? 'ELEVATED' : 'HEALTHY'; return Object.freeze({ queueId: queue.id, netGrowthPerMinute: net, estimatedDrainMinutes: drainMinutes, pressure: Math.round(pressure), state, scaleOut: state === 'CRITICAL' || state === 'HIGH' }); }
