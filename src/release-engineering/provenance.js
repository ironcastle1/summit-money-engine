import { digest } from './ids.js';
export function buildProvenance(input = {}) { const record = { builder: String(input.builder || 'merlin-release-engineering'), sourceRevision: String(input.sourceRevision || 'unknown'), sourceRepository: String(input.sourceRepository || 'local-upload'), buildCommand: String(input.buildCommand || 'npm run release:part18'), environment: String(input.environment || 'test'), startedAt: input.startedAt || null, completedAt: input.completedAt || new Date().toISOString(), materials: Array.isArray(input.materials) ? input.materials : [], subjects: Array.isArray(input.subjects) ? input.subjects : [] }; return Object.freeze({ ...record, attestation: digest(record) }); }
