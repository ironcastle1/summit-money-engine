export function releaseDiagnostics(snapshot = {}) { const candidate = snapshot.candidates?.[0] || null; return Object.freeze({ platform: 'MERLIN_RELEASE_ENGINEERING', version: '20.18.0', candidateId: candidate?.id || null, candidateState: candidate?.state || 'NONE', components: snapshot.components?.length || 0, contracts: snapshot.contracts?.length || 0, migrations: snapshot.migrations?.length || 0, artifacts: snapshot.artifacts?.length || 0, evidence: snapshot.evidence?.length || 0, readiness: snapshot.readiness?.state || 'NOT_RUN', goLiveDecision: snapshot.goLive?.decision || 'NOT_ASSESSED', generatedAt: new Date().toISOString() }); }
