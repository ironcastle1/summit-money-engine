export function supportMatrix(input = {}) { const rows = (input.clients || []).map(client => ({ client: client.name || client.id, minimumVersion: client.minimumVersion, currentVersion: client.currentVersion, browsers: client.browsers || [], platforms: client.platforms || [], state: client.state || 'SUPPORTED' })); return Object.freeze({ rows, node: input.node || '>=20', datastores: input.datastores || ['JSON document store'], regions: input.regions || [], unsupported: rows.filter(item => item.state === 'UNSUPPORTED') }); }
