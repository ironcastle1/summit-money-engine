services:
  - type: web
    name: summit-security-companion
    env: node
    plan: free
    buildCommand: npm install --package-lock=false --no-audit --no-fund
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.x
