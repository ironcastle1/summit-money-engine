MERLIN CNC V8.4 FULL REPOSITORY VERIFICATION
===========================================

Purpose of V8.4
- Fix the Render Node 20 startup crash caused by an old server-side image-to-DXF module importing CommonJS svg-path-parser with named ESM exports.
- Preserve the complete MERLIN V8 business OS.
- Preserve browser-based Image -> DXF generation.
- Preserve the V8.3 DXF Resizer and downloadable resized DXF workflow.

Verified in this build
- package.json version: 8.4.0
- server health version: 8.4.0
- server startup banner: V8.4
- src/design/image-to-dxf.js exists as a safe compatibility module and imports no svg-path-parser package
- public/image-dxf.js exists and remains the active Image -> DXF converter
- src/dxf/resize.js exists
- POST /api/tools/resize-dxf exists
- GET /api/tools/resize-dxf/:token/download exists
- 46 JavaScript files (including tests) passed node --check
- repository preflight passed: 40 runtime/frontend/script JavaScript files
- 0 missing relative imports
- all external imports detected by preflight are declared in package.json
- exact compatibility module was imported successfully under Node without the previous CommonJS/ESM exception
- total repository files before generated external manifest/verification: 83

Declared runtime dependencies
- better-sqlite3
- cheerio
- cors
- dotenv
- dxf-parser
- express
- multer

Important deployment observation from the supplied Render log
- Render reported "merlin-cnc@8.0.2", proving the failed deployment was not actually starting the V8.3 package.json.
- V8.4 overwrites the exact legacy path src/design/image-to-dxf.js so an old copy of that file cannot produce the makeAbsolute/svg-path-parser startup crash after the V8.4 files are copied over it.

Not claimed
- Full dependency-backed npm tests were not run in this sandbox because npm dependencies are not installed here.
- Render's build command installs dependencies and runs npm run check before starting the service.
