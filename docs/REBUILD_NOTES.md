# v4.4 Security correction build

Changes: strict war overlay, estimated conflict-zone layer, Guardian/tabloid filter, improved infrastructure search, UI key, no N/A estimate tiles, cleaned text entities, smaller dots, fixed place cards, and stronger radius scan estimates.
