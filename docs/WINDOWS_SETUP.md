# Windows

MERLIN targets Node 20.19.5. The repository contains `.node-version` so Render can select the correct Node major/version. Local setup is optional if you are deploying through GitHub + Render.
