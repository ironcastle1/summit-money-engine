# GitHub implementation

The quickest V2 update is to overwrite the active `main` working folder with the files in the supplied ZIP, commit all changes in GitHub Desktop, and push. Keep the existing `.git` folder and the `legacy-v25` branch.

Render should redeploy from the pushed `main` branch automatically.
