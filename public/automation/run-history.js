import { ageLabel, escapeHtml, stateClass } from './format.js';
export function renderRunHistory(root, runs = []) { root.innerHTML = `<section class="automation-panel"><header><div><span>EXECUTION LEDGER</span><h2>RUN HISTORY</h2></div></header><div class="automation-list">${runs.length ? runs.map(run => `<article class="automation-run"><div><b>${escapeHtml(run.workflowId)}</b><span>${run.steps?.length || 0} steps · ${Number(run.durationMs || 0)} ms</span><small>${escapeHtml(run.reason || run.trigger?.reason || 'Workflow execution')}</small></div><em class="${stateClass(run.state)}">${escapeHtml(run.state)}</em><time>${ageLabel(run.createdAt)}</time></article>`).join('') : '<div class="automation-empty">NO RUNS RECORDED</div>'}</div></section>`; }
