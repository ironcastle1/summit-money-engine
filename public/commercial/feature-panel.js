import { esc, tone } from './format.js';
export function featurePanelHtml(flags = [], releases = []) { return `<section class="commercial-card"><header><div><span>PRODUCT OPERATIONS</span><h2>Feature rollout</h2></div><button data-commercial-action="new-feature">NEW FLAG</button></header><div class="feature-list">${flags.map(item => `<article><div><b>${esc(item.name)}</b><small>${esc(item.key)}</small></div><i class="commercial-pill ${tone(item.rollout)}">${esc(item.rollout)}</i><span>${item.percentage || 0}%</span></article>`).join('')}</div><h3>Release notes</h3><div class="release-list">${releases.slice(0, 8).map(item => `<article><b>${esc(item.version)} · ${esc(item.title)}</b><p>${esc(item.summary)}</p></article>`).join('')}</div></section>`; }
