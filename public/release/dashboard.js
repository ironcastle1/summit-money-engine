import { escapeRelease, releaseNumber, releaseState } from './format.js';
export function releaseDashboardHtml(snapshot = {}) { const cards = [['READINESS', snapshot.readiness?.state || 'NOT RUN', `${releaseNumber(snapshot.readiness?.score)} score`], ['GO-LIVE', snapshot.goLive?.decision || 'NOT ASSESSED', `${snapshot.goLive?.blockers?.length || 0} blockers`], ['COMPONENTS', snapshot.components?.length || 0, `${snapshot.dependencies?.issues?.length || 0} dependency issues`], ['CONTRACTS', snapshot.contracts?.length || 0, `${snapshot.contractInventory?.duplicates?.length || 0} duplicates`], ['MIGRATIONS', snapshot.migrations?.length || 0, snapshot.migrationAssessment?.valid ? 'plan valid' : 'review required'], ['EVIDENCE', snapshot.evidence?.length || 0, `${snapshot.evidence?.filter(item => item.state === 'PASS').length || 0} passing`]]; return `<div class="release-scorecards">${cards.map(([label, value, note]) => `<article class="${releaseState(value)}"><span>${escapeRelease(label)}</span><strong>${escapeRelease(value)}</strong><small>${escapeRelease(note)}</small></article>`).join('')}</div>`; }
