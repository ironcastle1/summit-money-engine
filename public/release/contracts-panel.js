import { escapeRelease } from './format.js';
export function contractsPanelHtml(snapshot = {}) { return `<section class="release-panel"><header><h2>API CONTRACTS</h2><span>${snapshot.contracts?.length || 0} ROUTES</span></header><div class="release-list compact">${(snapshot.contracts || []).slice(0, 20).map(item => `<article><span class="release-method">${escapeRelease(item.method)}</span><div><b>${escapeRelease(item.path)}</b><small>${escapeRelease(item.auth)} · ${escapeRelease(item.version)}</small></div></article>`).join('') || '<p>No explicit contracts recorded.</p>'}</div></section>`; }
