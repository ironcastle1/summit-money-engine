MERLIN CNC V8.9 FULL REPOSITORY VERIFICATION

Version checks
- package.json: 8.9.0
- /api/health: 8.9.0
- server startup banner: MERLIN CNC V8.9

Repository/preflight
- node scripts/preflight.js: PASS
- JavaScript syntax checked by preflight: 41 files
- external dependencies declared: better-sqlite3, cheerio, cors, dotenv, dxf-parser, express, multer
- required repository files present: 13/13
- public/image-dxf.js: node --check PASS
- public/app.js: node --check PASS
- src/routes/index.js: node --check PASS
- server.js: node --check PASS

V8.9 image-to-DXF changes
- Image type selector: Auto / Line drawing or separated details / Solid silhouette
- Otsu and global threshold candidates retained
- background colour-distance masks retained
- local-contrast masks added
- thin-stroke boldening passes added
- multi-component extraction added
- deterministic component bridging added
- bridge distance and component-size filters added
- joined geometry is re-run through largest-connected-component extraction
- connectivity rescue reports bridges_added and components_joined
- R12 AC1009 output retained

V8.9 UI regression tests
- Image-to-DXF line-art rescue controls: PASS
- No fixing holes remains the default DXF Resizer option: PASS
- 4/4 targeted V8.8/V8.9 UI tests passed

Important limitation
Arbitrary photographs do not contain an unambiguous CNC cut plan. V8.9 is materially more tolerant of line drawings, separated strokes and fragmented foreground artwork, but it still refuses output when it cannot deterministically isolate enough usable geometry. The original image should be retained and the generated DXF preview inspected before cutting.

Full dependency-backed tests were not executed in this container because node_modules are not installed here. Render installs dependencies and is configured to run npm run check and npm test before startup.
