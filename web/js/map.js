(function(){
let map, countryLayer, conflictLayer, eventLayer, cityLayer, selectedLayer, areaScanLayer, countryGeo;
let state={}, mapData={}, lang={dotsOn:'DOTS ON',dotsOff:'DOTS OFF',key:'KEY',collapse:'MINIMISE',expand:'KEY',liveAlert:'LIVE ALERT',openOnMap:'OPEN ON MAP'};
const overlays={war:true,crisis:false,watch:false};
let dotsOn=true, legendCollapsed=false;
const colors={war:'#ff174f',terror:'#ff8c00',earthquake:'#ffffff',crisis:'#ffffff',politics:'#b24cff',movement:'#00d8ff',money:'#ffd94a',security:'#3ea0ff',city:'#7aa7ff'};
const featureSymbols={movement:'↔',money:'£',politics:'⚑',hospital:'🏥',clinic:'⚕',pharmacy:'✚',police_station:'★',embassy_consulate:'⚑',airport_airfield:'✈',fuel_station:'⛽',border_crossing:'⇄',port_harbour:'⚓',rail_station:'▣',main_road:'═'};
function $(id){return document.getElementById(id)}
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));}
function init(){map=L.map('map',{preferCanvas:true,minZoom:2,maxZoom:16,zoomControl:true,worldCopyJump:false,maxBounds:[[-85,-180],[85,180]],maxBoundsViscosity:1}).setView([34,16],3);window.map=map;L.tileLayer('https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',{subdomains:'abcd',attribution:'&copy; OpenStreetMap &copy; CARTO',noWrap:true,bounds:[[-85,-180],[85,180]],keepBuffer:3}).addTo(map);countryLayer=L.layerGroup().addTo(map);conflictLayer=L.layerGroup().addTo(map);eventLayer=L.layerGroup().addTo(map);cityLayer=L.layerGroup().addTo(map);selectedLayer=L.layerGroup().addTo(map);areaScanLayer=L.layerGroup().addTo(map);bind();loadBoundaries();map.on('click',e=>openPlace(e.latlng.lat,e.latlng.lng));map.on('zoomend moveend',renderCities);setTimeout(()=>map.invalidateSize(),300)}
function bind(){const h=$('homeBtn');if(h)h.onclick=()=>map.setView([34,16],3);const d=$('dotBtn');if(d)d.onclick=()=>{dotsOn=!dotsOn;d.textContent=dotsOn?(lang.dotsOn||'DOTS ON'):(lang.dotsOff||'DOTS OFF');renderEvents();renderCities();};const o=$('overlayBtn');if(o)o.onclick=overlayPanel;const r=$('refreshBtn');if(r)r.onclick=async()=>{Renderers.panel('Refreshing',`<div class="card"><h3>Refreshing sources</h3><div class="loader"><span></span></div></div>`);await API.refresh();const [s,m]=await Promise.all([API.state(),API.mapData()]);setData(m,s);Renderers.liveBrief(s.liveBrief);};}
function setLanguage(t){lang=t||lang;const d=$('dotBtn');if(d)d.textContent=dotsOn?(lang.dotsOn||'DOTS ON'):(lang.dotsOff||'DOTS OFF');legend();}
async function loadBoundaries(){try{countryGeo=await API.json('/api/boundaries/admin0');renderCountries();}catch(e){console.warn(e)}}
function setData(md,st){mapData=md||{};state=st||{};renderCountries();renderConflictZones();renderEvents();renderCities();renderTicker();}
function cName(f){const p=f.properties||{};return p.ADMIN||p.NAME_EN||p.name||p.NAME||'';}
function cRisk(name){const n=String(name||'').toLowerCase();return (state.countryRisk||mapData.countryRisk||[]).find(r=>{const c=String(r.country||'').toLowerCase();return c&&(n===c||n.includes(c)||c.includes(n));});}
function renderCountries(){if(!countryGeo||!countryLayer)return;countryLayer.clearLayers();L.geoJSON(countryGeo,{style:f=>{const name=cName(f),r=cRisk(name);let fill='#00a66a',op=.010,line=.08,w=.5;if(overlays.war&&r&&r.overlayWar){fill='#760016';op=.42;line=.9;w=1.4}else if(overlays.crisis&&r&&r.crisis>0){fill='#ff8c00';op=.12;line=.32;w=.8}else if(overlays.watch&&r&&r.watch){fill='#ffd94a';op=.13;line=.38;w=.8}return{color:fill,weight:w,opacity:line,fillColor:fill,fillOpacity:op,className:'country-fill'}},onEachFeature:(f,l)=>l.on('click',e=>{L.DomEvent.stopPropagation(e);openPlace(e.latlng.lat,e.latlng.lng);})}).addTo(countryLayer)}
function renderConflictZones(){if(!conflictLayer)return;conflictLayer.clearLayers();if(!overlays.war)return;(mapData.conflictZones||[]).forEach(z=>{let layer;if(z.polygon&&Array.isArray(z.polygon)){layer=L.polygon(z.polygon,{color:'#ff174f',fillColor:'#760016',fillOpacity:.48,weight:2,className:'conflict-zone'});}else if(Number.isFinite(+z.lat)&&Number.isFinite(+z.lng)){layer=L.circle([+z.lat,+z.lng],{radius:(+z.radiusKm||50)*1000,color:'#ff174f',fillColor:'#760016',fillOpacity:.30,weight:2,className:'conflict-zone'});}if(!layer)return;layer.on('click',ev=>{L.DomEvent.stopPropagation(ev);Renderers.panel('Estimated conflict area',`<div class="card"><h3>${esc(z.name||'Conflict area')}</h3><div class="list"><div class="item"><b>Country:</b> ${esc(z.country||'mapped')}</div><div class="item"><b>Meaning:</b> estimated public-source conflict/security area. Not a verified military frontline.</div><div class="item yellow"><b>Use:</b> warning layer for travellers and people in active conflict zones. Check local contacts, official advice and current reports before movement.</div></div></div>`);});layer.addTo(conflictLayer);});}
function kind(input){const k=String(input||'security').toLowerCase();if(k==='earthquake'||k==='quake')return'earthquake';if(['weather','disaster'].includes(k))return'crisis';return k;}
function icon(k0,subtype){const k=subtype==='earthquake'?'earthquake':kind(k0);if(k==='earthquake')return L.divIcon({className:'',html:`<div class="dot earthquake">⌁</div>`,iconSize:[15,15],iconAnchor:[8,8]});if(featureSymbols[k])return L.divIcon({className:'',html:`<div class="map-symbol" style="background:${colors[k]||colors.security};box-shadow:0 0 7px ${colors[k]||colors.security}">${esc(featureSymbols[k])}</div>`,iconSize:[15,15],iconAnchor:[8,8]});const col=colors[k]||colors.security;return L.divIcon({className:'',html:`<div class="dot" style="background:${col};box-shadow:0 0 6px ${col}"></div>`,iconSize:[7,7],iconAnchor:[4,4]});}
function renderEvents(){eventLayer.clearLayers();if(!dotsOn)return;(state.events||[]).filter(e=>e.displayOnMap!==false).forEach(e=>{if(!Number.isFinite(+e.lat)||!Number.isFinite(+e.lng))return;if(e.subtype==='earthquake'&&Number(e.magnitude||0)<5)return;L.marker([+e.lat,+e.lng],{icon:icon(e.kind,e.subtype)}).on('click',ev=>{L.DomEvent.stopPropagation(ev);Renderers.eventCard(e)}).addTo(eventLayer);});}
function renderCities(){cityLayer.clearLayers();if(!dotsOn||map.getZoom()<7)return;const b=map.getBounds();(mapData.cityNodes||[]).filter(c=>b.pad(.25).contains([c.lat,c.lng])).slice(0,220).forEach(c=>L.marker([c.lat,c.lng],{icon:icon('city')}).on('click',ev=>{L.DomEvent.stopPropagation(ev);openPlace(c.lat,c.lng)}).addTo(cityLayer));}
async function openPlace(lat,lng){selectedLayer.clearLayers();L.circleMarker([lat,lng],{radius:6,color:'#00eaff',weight:2,fillOpacity:.25}).addTo(selectedLayer);Renderers.panel(lang.placeRisk||'Place Risk',`<div class="card"><h3>${lang.loading||'Loading'}...</h3><div class="loader"><span></span></div><p class="plain">Loading exact place, infrastructure, weather, live signals and image.</p></div>`);try{const data=await API.place(lat,lng);Renderers.placeCard(data)}catch(e){Renderers.panel('Place failed',`<div class="card"><h3>Place data failed</h3><p class="plain">${esc(e.message)}</p></div>`)} }
function overlayPanel(){Renderers.panel('Map overlays',`<div class="panel-subhead">War starts on. Crisis and watch start off.</div><div class="card"><div class="list"><label class="item"><input id="ovWar" type="checkbox" ${overlays.war?'checked':''}> War / active conflict country colour</label><label class="item"><input id="ovCrisis" type="checkbox" ${overlays.crisis?'checked':''}> Serious crisis country colour</label><label class="item"><input id="ovWatch" type="checkbox" ${overlays.watch?'checked':''}> Watchlist country colour</label><div class="item yellow"><b>Rule:</b> dark red is active conflict/watch conflict context. Polygons are estimates, not exact frontlines.</div></div></div>`);$('ovWar').onchange=e=>{overlays.war=e.target.checked;renderCountries();renderConflictZones();};$('ovCrisis').onchange=e=>{overlays.crisis=e.target.checked;renderCountries();};$('ovWatch').onchange=e=>{overlays.watch=e.target.checked;renderCountries();};}
function drawAreaScan(scan){areaScanLayer.clearLayers();if(!scan||!scan.target)return;const lat=+scan.target.lat,lng=+scan.target.lng,km=+scan.radiusKm||8.04672;map.setView([lat,lng],Math.max(12,Math.min(14,map.getZoom()+2)));L.circle([lat,lng],{radius:km*1000,color:'#00eaff',fillColor:'#00eaff',fillOpacity:.12,weight:3,className:'area-scan-circle'}).addTo(areaScanLayer);L.marker([lat,lng],{icon:icon('city')}).addTo(areaScanLayer);(scan.infrastructure&&scan.infrastructure.items||[]).slice(0,120).forEach(item=>{if(!Number.isFinite(+item.lat)||!Number.isFinite(+item.lng))return;L.marker([+item.lat,+item.lng],{icon:infraIcon(item.type)}).on('click',ev=>{L.DomEvent.stopPropagation(ev);Renderers.panel(item.name||'Infrastructure',`<div class="card"><h3>${esc(item.name||item.type)}</h3><div class="list"><div class="item"><b>Type:</b> ${esc((item.type||'').replace(/_/g,' '))}</div><div class="item"><b>Distance:</b> ${Number(item.distanceMiles||0).toFixed(2)} miles</div><div class="item"><b>Source:</b> OpenStreetMap/Overpass</div></div></div>`)}).addTo(areaScanLayer);});(scan.insideEvents||[]).slice(0,80).forEach(e=>{if(!Number.isFinite(+e.lat)||!Number.isFinite(+e.lng))return;L.marker([+e.lat,+e.lng],{icon:icon(e.kind,e.subtype)}).on('click',ev=>{L.DomEvent.stopPropagation(ev);Renderers.eventCard(e)}).addTo(areaScanLayer);});}
function infraIcon(type){return L.divIcon({className:'',html:`<div class="feature-marker">${featureSymbols[type]||'•'}</div>`,iconSize:[18,18],iconAnchor:[9,9]});}
function renderTicker(){const el=$('ticker');if(!el)return;const rows=(state.markets||[]).filter(m=>m.changePct!==null&&m.changePct!==undefined&&Number.isFinite(Number(m.changePct))&&Number.isFinite(Number(m.price))).slice(0,8);el.innerHTML=rows.map(m=>{const ch=Number(m.changePct);const cls=ch>0?'up':ch<0?'down':'flat';const arrow=ch>0?'▲':ch<0?'▼':'•';return `<span class="tick ${cls}">${esc(m.id||m.name)} ${arrow} ${ch.toFixed(2)}%</span>`;}).join('')||'No live market data';}
function legend(){const el=$('legend');if(!el)return;if(legendCollapsed){el.innerHTML=`<button id="legendToggle" class="legend-toggle">${esc(lang.expand||'KEY')}</button>`;$('legendToggle').onclick=()=>{legendCollapsed=false;legend();};return;}const rows=[['dark red conflict area','area','#760016'],[lang.war||'war','dot',colors.war],[lang.terror||'terror','dot',colors.terror],[lang.earthquake||'earthquake','earth','⌁'],[lang.crisisWord||'crisis','dot',colors.crisis],[lang.politics||'politics','symbol','⚑'],[lang.movement||'movement','symbol','↔'],[lang.money||'money','symbol','£'],[lang.hospital||'hospital','symbol','🏥'],[lang.pharmacy||'pharmacy','symbol','✚'],[lang.police||'police','symbol','★'],[lang.airport||'airport','symbol','✈'],[lang.fuel||'fuel','symbol','⛽'],[lang.border||'border','symbol','⇄'],[lang.port||'port','symbol','⚓'],[lang.rail||'rail','symbol','▣'],[lang.road||'main road','symbol','═'],[lang.city||'city','dot',colors.city]];el.innerHTML=`<button id="legendToggle" class="legend-toggle">${esc(lang.collapse||'MINIMISE')}</button>`+rows.map(([name,type,val])=>type==='area'?`<span><i style="background:${val};border:1px solid #ff174f"></i>${esc(name)}</span>`:type==='earth'?`<span><b class="legend-symbol">${val}</b>${esc(name)}</span>`:type==='symbol'?`<span><b class="legend-symbol">${val}</b>${esc(name)}</span>`:`<span><i style="background:${val}"></i>${esc(name)}</span>`).join('');$('legendToggle').onclick=()=>{legendCollapsed=true;legend();};}
function liveAlert(event){const toast=$('toast');if(!toast||!event)return;toast.innerHTML=`<button class="toast-close" type="button">×</button><div class="a-title">${esc(lang.liveAlert||'LIVE ALERT')}</div><div class="a-meta">${esc(event.title||event.summary||'New security signal')}</div><button id="toastOpen" type="button">${esc(lang.openOnMap||'OPEN ON MAP')}</button>`;toast.classList.add('show');toast.querySelector('.toast-close').onclick=()=>toast.classList.remove('show');toast.querySelector('#toastOpen').onclick=()=>{if(event.lat&&event.lng)map.setView([event.lat,event.lng],Math.max(map.getZoom(),8));Renderers.eventCard(event);};setTimeout(()=>toast.classList.remove('show'),22000);}
window.MoneyMap={init,setData,legend,drawAreaScan,setLanguage,liveAlert};
})();
