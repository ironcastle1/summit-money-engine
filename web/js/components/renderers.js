window.Renderers = (() => {
  const money = n => Number.isFinite(Number(n)) ? '$' + Number(n).toLocaleString(undefined,{maximumFractionDigits:Number(n)<10?4:2}) : '—';
  const pct = n => `<span class="${Number(n)>=0?'hot':'bad'}">${Number(n)>=0?'+':''}${Number(n||0).toFixed(2)}%</span>`;
  const esc = s => String(s??'').replace(/[&<>]/g,m=>({ '&':'&amp;','<':'&lt;','>':'&gt;' }[m]));
  function renderTicker(markets){ const row = (markets||[]).slice(0,14).map(m => `${m.id} ${money(m.price)} ${Number(m.changePct)>=0?'▲':'▼'}${Math.abs(Number(m.changePct||0)).toFixed(2)}%`).join('  |  '); document.getElementById('ticker').textContent = row || 'No market data'; document.getElementById('marquee').textContent = row + ' /// ' + row; }
  function renderMarkets(markets){ const el=document.getElementById('markets'); el.innerHTML=(markets||[]).map(m=>`<div class="market-row"><div class="sym">${esc(m.id)}</div><div><div class="name">${esc(m.name)}</div><div class="sub">${esc(m.source)} · ${esc(m.status)} ${m.ageSec?`· ${m.ageSec}s old`:''}</div></div><div class="price"><b>${money(m.price)}</b><br>${pct(m.changePct)}</div></div>`).join('') || '<div class="sub">No market data yet.</div>'; }
  function renderPolymarket(items){ const el=document.getElementById('poly'); el.innerHTML=(items||[]).slice(0,14).map(p=>`<div class="poly-card"><b>${esc(p.question)}</b><p>Volume: ${Math.round(p.volume||0).toLocaleString()} · Liquidity: ${Math.round(p.liquidity||0).toLocaleString()}</p><a target="_blank" href="${esc(p.url)}">source</a></div>`).join('') || '<div class="sub">No Polymarket data yet.</div>'; }
  function renderSignals(items){ const el=document.getElementById('signals'); el.innerHTML=(items||[]).map(s=>`<div class="signal-card"><h4>${esc(s.title)}</h4><p><span class="score">Score ${Math.round(s.score)}</span> · ${esc(s.status)} · ${esc(s.source)}</p><p>${esc(s.why)}</p><p><b>Watch:</b> ${esc(s.watch)}</p><p><b>Checks:</b> ${(s.checks||[]).map(esc).join(' · ')}</p>${s.url&&s.url!=='#'?`<a target="_blank" href="${esc(s.url)}">source</a>`:''}</div>`).join('') || '<div class="sub">No signals yet.</div>'; }
  function renderRapid(items){ const el=document.getElementById('rapid'); el.innerHTML=(items||[]).map((r,i)=>`<div class="rapid-card"><h4>${esc(r.asset)} · ${esc(r.label)}</h4><p><b>Move:</b> ${esc(r.move15)}% short window · ${esc(r.moveDay)}% session · rank ${esc(r.rank)}</p><p><b>Estimate:</b> continuation ${esc(r.continuation)}% · drop/reversal ${esc(r.drop)}%</p><p><b>Why listed:</b> ${(r.reasons||[]).map(esc).join(' · ')}</p><p><b>Checks:</b> ${(r.checks||[]).map(esc).join(' · ')}</p><div class="chart-box"><b>${esc(r.asset)} projection</b><canvas id="rapidChart${i}"></canvas></div></div>`).join('') || '<div class="sub">No rapid movers yet.</div>'; setTimeout(()=>items?.forEach((r,i)=>window.Charts?.line(`rapidChart${i}`, r.projection||[], r.asset, 'Projected price / probability path')),50); }
  async function imageFor(name){ try{ const r=await fetch('/api/image?title='+encodeURIComponent(name)); const d=await r.json(); return d.ok && d.image ? `<img class="ctx-image" src="${esc(d.image)}" alt="${esc(d.title)}"><div class="source-line">image/source: <a target="_blank" href="${esc(d.source)}">Wikipedia/Wikimedia</a></div>` : '<div class="warn">No verified image available. Not showing stock filler.</div>'; }catch(e){ return '<div class="warn">No verified image available.</div>'; } }
  async function renderNode(n){ const img=await imageFor(n.name.split('/')[0].replace(/Port|Belt|Node|Hub|Route|Risk/gi,'').trim()); const html=`${img}<div class="ctx-card"><h3>${esc(n.name)}</h3><p>${esc(n.note)}</p><p><b>Watch:</b> ${(n.watch||[]).map(esc).join(', ')}</p><p class="source-line">${esc(n.source||'baseline map node')}</p></div>`; openSide('Map node',html); }
  function renderRoute(r){ const html=`<div class="ctx-card"><h3>${esc(r.name)}</h3><p><b>Goods:</b> ${esc(r.goods)}</p><p><b>Watch:</b> ${(r.watch||[]).map(esc).join(', ')}</p><p><b>Use:</b> compare route disruption reports with freight, fuel and listed shipping/insurance assets. Do not trade from the map alone.</p></div>`; openSide('Trade route',html); }
  function renderEvent(e){ const html=`<div class="ctx-card"><h3>${esc(e.title)}</h3><p>${esc(e.summary||'')}</p><p><b>Place:</b> ${esc(e.place)}</p><p><b>Watch:</b> ${(e.watch||[]).map(esc).join(', ')}</p><p class="source-line">${esc(e.source)} · ${esc(e.time||'')}</p>${e.url&&e.url!=='#'?`<a target="_blank" href="${esc(e.url)}">source</a>`:''}</div>`; openSide('Event detail',html); }
  async function renderContext(data){ if(!data) return; const title=data.reverse?.place?.split(',').slice(0,2).join(', ') || data.country?.name || 'Area'; const img=await imageFor(title.split(',')[0]); const html=`${img}<div class="ctx-card"><p>${esc(data.country?.summary||'')}</p><p><b>Watch:</b> ${(data.country?.watches||[]).map(esc).join(', ')}</p><p class="source-line">${esc(data.note||'')}</p></div><h3>Nearby events</h3>${(data.nearEvents||[]).slice(0,12).map(e=>`<div class="ctx-card"><b>${esc(e.title)}</b><p>${esc(e.source)} · ${esc(e.place)}</p>${e.url&&e.url!=='#'?`<a target="_blank" href="${esc(e.url)}">source</a>`:''}</div>`).join('') || '<div class="warn">No verified live events near this point yet.</div>'}<h3>Nearby cities/nodes</h3>${[...(data.nearCities||[]),...(data.nearNodes||[])].slice(0,12).map(n=>`<div class="ctx-card"><b>${esc(n.name)}</b><p>${esc(n.note)}</p><p>Watch: ${(n.watch||[]).map(esc).join(', ')}</p></div>`).join('')}`; openSide(title,html); }
  function openSide(title, html){ document.getElementById('contextTitle').textContent=title; document.getElementById('contextBody').innerHTML=html; Panels.open('context'); }
  return { renderTicker, renderMarkets, renderPolymarket, renderSignals, renderRapid, renderContext, renderNode, renderRoute, renderEvent };
})();
