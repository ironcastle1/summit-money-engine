export function workflowFixture(overrides = {}) { return { name: 'Critical workflow', state: 'ACTIVE', triggers: [{ type: 'DECISION_SIGNAL', configuration: { minimumScore: 70 } }], actions: [{ id: 'notify', type: 'SEND_NOTIFICATION', configuration: { title: '{{signal.title}}', body: '{{signal.summary}}', severity: 'URGENT', channels: ['IN_APP'] } }, { id: 'task', type: 'CREATE_TASK', dependsOn: ['notify'], configuration: { title: 'Review {{signal.title}}', priority: 80 } }], ...overrides }; }
export function signalFixture(overrides = {}) { return { id: 'signal-1', domain: 'CONFLICT', title: 'Material signal', summary: 'A material development requires review.', attention: { score: 86, band: 'CRITICAL' }, location: { lat: 51.5, lon: -0.1 }, ...overrides }; }
export function decisionSupportStub() { let sequence = 0; const records = { tasks: [], cases: [], reports: [], notes: [], approvals: [] }; return { records, activity: { putTask: async (_owner, input) => { const item = { id: `task-${++sequence}`, ...input }; records.tasks.push(item); return item; }, putNote: async (_owner, input) => { const item = { id: `note-${++sequence}`, ...input }; records.notes.push(item); return item; } }, cases: { put: async (_owner, input) => { const item = { id: `case-${++sequence}`, ...input }; records.cases.push(item); return item; }, attach: async () => true }, report: async (input) => { const item = { id: `report-${++sequence}`, type: input.type, title: input.title, metadata: { signalCount: 0 } }; records.reports.push(item); return item; }, approvals: { create: async (_owner, input) => { const item = { id: `approval-${++sequence}`, ...input }; records.approvals.push(item); return item; } } }; }
