import test from 'node:test';import assert from 'node:assert/strict';import {buildOverlayLegend} from '../../src/overlays/legend-builder.js';test('legend uses explicit layer legend',()=>{const legend=buildOverlayLegend({legend:[{label:'A',colour:'#fff'}]},[]);assert.equal(legend[0].label,'A');});test('legend derives categories when absent',()=>{const legend=buildOverlayLegend({style:{colour:'#000'}},[{properties:{category:'war'}},{properties:{category:'war'}},{properties:{category:'flood'}}]);assert.equal(legend[0].count,2);});
