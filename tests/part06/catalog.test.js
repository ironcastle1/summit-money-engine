import test from 'node:test';import assert from 'node:assert/strict';import {OverlayCatalog} from '../../src/overlays/catalog.js';import {DEFAULT_OVERLAYS} from '../../src/overlays/default-catalog.js';test('catalog contains market-ready overlay catalogue',()=>{const catalog=new OverlayCatalog(DEFAULT_OVERLAYS);assert.ok(catalog.list().length>=50);assert.equal(catalog.get('active-wars').group,'conflict');assert.equal(catalog.get('shipping-routes').group,'logistics');assert.equal(catalog.groups().length,10);const earthquakes=catalog.get('major-earthquakes');assert.equal(earthquakes.filters.materialEarthquakesOnly,true);assert.deepEqual(earthquakes.filters.categories,['earthquake']);});
