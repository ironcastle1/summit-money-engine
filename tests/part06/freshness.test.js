import test from 'node:test';import assert from 'node:assert/strict';import {freshnessState} from '../../src/overlays/freshness.js';test('freshness distinguishes fresh stale and expired',()=>{const now=Date.parse('2026-08-04T12:00:00Z');const layer={sourceMode:'live',refreshSeconds:60};assert.equal(freshnessState(layer,'2026-08-04T11:59:00Z',now).state,'fresh');assert.equal(freshnessState(layer,'2026-08-04T11:00:00Z',now).state,'expired');});
