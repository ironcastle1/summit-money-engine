import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('overlay API routes are registered',async()=>{const routes=await readFile('src/api/register-overlay-routes.js','utf8');const app=await readFile('src/app/create-application.js','utf8');for(const endpoint of ['/api/overlays/catalog','/api/overlays/presets','/api/overlays/availability','/api/overlays/query'])assert.ok(routes.includes(endpoint));assert.match(app,/registerOverlayRoutes/);assert.match(app,/createOverlayPlatformService/);});
