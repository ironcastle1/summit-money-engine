import test from 'node:test';import assert from 'node:assert/strict';import {earthquakeMateriality,retainMaterialEarthquake} from '../../src/overlays/material-earthquake-gate.js';test('routine earthquakes are removed',()=>{assert.equal(retainMaterialEarthquake({category:'earthquake',magnitude:4.8}),false);assert.equal(retainMaterialEarthquake({category:'earthquake',magnitude:6.2}),true);assert.equal(retainMaterialEarthquake({category:'earthquake',magnitude:5.1,shippingImpact:true}),true);assert.deepEqual(earthquakeMateriality({category:'storm'}).reasons,['NOT_EARTHQUAKE']);});
