import test from 'node:test';import assert from 'node:assert/strict';import {overlayDefinition} from '../../src/overlays/definition.js';test('definition validates IDs and renderer',()=>{assert.throws(()=>overlayDefinition({id:'BAD',renderer:'marker',sourceMode:'live'}));assert.throws(()=>overlayDefinition({id:'good-id',renderer:'bad',sourceMode:'live'}));const layer=overlayDefinition({id:'good-id',renderer:'marker',sourceMode:'live'});assert.equal(layer.id,'good-id');assert.ok(Object.isFrozen(layer));});
