import test from 'node:test';import assert from 'node:assert/strict';import {normalizeOverlayFeature,featureCollection} from '../../src/overlays/feature-normalizer.js';test('records become attributed GeoJSON',()=>{const layer={id:'ports',sourceMode:'static',title:'Ports'};const feature=normalizeOverlayFeature({id:'p1',name:'Port',lat:50,lon:-2},layer);assert.deepEqual(feature.geometry.coordinates,[-2,50]);assert.equal(feature.properties.overlayId,'ports');assert.equal(featureCollection([feature]).features.length,1);});
