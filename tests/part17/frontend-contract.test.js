import test from 'node:test';
import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
const read = path => readFile(new URL(`../../${path}`, import.meta.url), 'utf8');
test('operations workspace is linked from the main shell', async () => { const [index, merlin] = await Promise.all([read('public/index.html'), read('public/merlin.js')]); assert.match(index, /data-view="operations"/); assert.match(index, /reliability-v20\.css\?v=20\.18\.0/); assert.match(index, /merlin\.js\?v=20\.20\.0/); assert.match(merlin, /installReliabilitySystem/); assert.match(merlin, /view === 'operations'/); });
test('operations browser modules expose operational controls without placeholders', async () => { const [controller, incident, recovery, deployment] = await Promise.all([read('public/reliability/controller.js'), read('public/reliability/incident-panel.js'), read('public/reliability/recovery-panel.js'), read('public/reliability/deployment-panel.js')]); assert.match(incident, /DECLARE INCIDENT/); assert.match(recovery, /RESTORE EVIDENCE/); assert.match(deployment, /RELEASE CONTROL/); assert.doesNotMatch(controller + incident + recovery + deployment, /TODO|placeholder|coming soon/i); });
