import test from'node:test';import assert from'node:assert/strict';import{LiveDataPlatform}from'../../src/live-data/live-data-platform.js';
function store(){let doc={sources:{},runs:[]};return{load:async()=>doc,read:async()=>structuredClone(doc),source:async id=>doc.sources[id]||null,saveSource:async(id,value)=>(doc.sources[id]=value),saveRun:async run=>(doc.runs.unshift(run),run),close:async()=>{}};}
test('platform refreshes registered public connectors and reports coverage',async()=>{const platform=new LiveDataPlatform({store:store(),autoStart:false});platform.register('ecb-fx',{fetch:async()=>({records:[{rate:1.1}]})});await platform.initialize();const snapshot=await platform.refresh();assert.equal(snapshot.sources['ecb-fx'].state,'ONLINE');assert.ok(snapshot.coverage.score>=0);await platform.close();});
