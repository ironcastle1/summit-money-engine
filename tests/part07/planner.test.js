import test from 'node:test';import assert from 'node:assert/strict';import { RouteGraph } from '../../src/logistics/route-graph.js';import { planAlternatives } from '../../src/logistics/alternative-router.js';
test('planner ranks viable alternatives according to policy',()=>{const graph=new RouteGraph();for(const [id,lon] of [['a',0],['b',5],['c',5],['d',10]])graph.addNode({id,name:id,kind:'PORT',coordinates:{lat:0,lon},importance:60});for(const edge of [{id:'ab',from:'a',to:'b',distanceKm:600},{id:'bd',from:'b',to:'d',distanceKm:600},{id:'ac',from:'a',to:'c',distanceKm:800},{id:'cd',from:'c',to:'d',distanceKm:800}])graph.addEdge({...edge,name:edge.id,mode:'SEA',baseSpeedKmh:30,importance:60,coordinates:[[0,0],[5,0]],restrictions:{},metadata:{}});const request={originId:'a',destinationId:'d',vesselClass:'PANAMAX',cargoClass:'CONTAINERS',policyId:'BALANCED',cargoTonnes:10000,departureAt:'2026-08-04T00:00:00Z',avoidNodeIds:[],avoidRouteIds:[],maximumAlternatives:3};const routes=planAlternatives(graph,request,{});assert.ok(routes.length>=2);assert.equal(routes[0].rank,1);assert.equal(routes[0].recommended,true);assert.ok(routes[0].metrics.cost.totalUsd>0);});
