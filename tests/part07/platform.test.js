import test from 'node:test';import assert from 'node:assert/strict';import { RouteGraph } from '../../src/logistics/route-graph.js';import { LogisticsPlatform } from '../../src/logistics/logistics-platform.js';
test('platform resolves coordinate endpoints and returns comparison and geojson',async()=>{const graph=new RouteGraph();for(const [id,lon] of [['a',0],['b',5],['c',10]])graph.addNode({id,name:id,kind:'PORT',coordinates:{lat:0,lon},importance:60});for(const edge of [{id:'ab',from:'a',to:'b'},{id:'bc',from:'b',to:'c'}])graph.addEdge({...edge,name:edge.id,mode:'SEA',distanceKm:600,baseSpeedKmh:30,importance:60,coordinates:[[0,0],[5,0]],restrictions:{},metadata:{}});const platform=new LogisticsPlatform({catalog:{ports:[],chokepoints:[],routes:{features:[]}},graph});const result=await platform.plan({origin:{lat:0,lon:.1},destination:{lat:0,lon:9.9},vesselClass:'PANAMAX',cargoClass:'CONTAINERS',policyId:'BALANCED',cargoTonnes:1000,maximumAlternatives:2});assert.equal(result.request.originId,'a');assert.equal(result.request.destinationId,'c');assert.equal(result.routes.length,1);assert.equal(result.geojson.features.length,2);assert.equal(platform.diagnostics().status,'READY');});
