import test from 'node:test';import assert from 'node:assert/strict';import { RouteGraph } from '../../src/logistics/route-graph.js';import { LogisticsPlatform } from '../../src/logistics/logistics-platform.js';
test('scenario closes routes and produces propagated network impact',async()=>{const graph=new RouteGraph();for(const [id,lon] of [['a',0],['b',5],['c',5],['d',10]])graph.addNode({id,name:id,kind:'PORT',coordinates:{lat:0,lon},importance:70});for(const edge of [{id:'ab',from:'a',to:'b'},{id:'bd',from:'b',to:'d'},{id:'ac',from:'a',to:'c'},{id:'cd',from:'c',to:'d'}])graph.addEdge({...edge,name:edge.id,mode:'SEA',distanceKm:700,baseSpeedKmh:30,importance:60,coordinates:[[0,0],[5,0]],restrictions:{},metadata:{}});const platform=new LogisticsPlatform({catalog:{ports:[],chokepoints:[],routes:{features:[]}},graph});const result=await platform.scenario({name:'Close northern path',request:{originId:'a',destinationId:'d',vesselClass:'PANAMAX',cargoClass:'CONTAINERS',policyId:'BALANCED',cargoTonnes:1000,maximumAlternatives:3},closedRouteIds:['ab']});assert.equal(result.name,'Close northern path');assert.ok(result.routes.every(route=>!route.path.edges.some(edge=>edge.id==='ab')));assert.ok(result.propagation.impacts.length===0);});
