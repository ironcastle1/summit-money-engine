import test from 'node:test';import assert from 'node:assert/strict';import { MemorySavedRouteRepository } from '../../src/logistics/saved-route-repository.js';import { RouteWatchlist } from '../../src/logistics/route-watchlist.js';import { evaluateRouteAlerts } from '../../src/logistics/alert-evaluator.js';
test('route watchlists persist thresholds and emit quantified alerts',async()=>{const repository=new MemorySavedRouteRepository();const watchlist=new RouteWatchlist(repository);const watch=await watchlist.add('u1',{routeId:'r1',name:'Route one',thresholds:{riskScore:60,etaChangeHours:10,costChangePct:15}});assert.equal((await watchlist.list('u1')).length,1);const alerts=evaluateRouteAlerts([watch],[{routeId:'r1',riskScore:75,etaChangeHours:14,costChangePct:4}]);assert.equal(alerts.length,2);assert.ok(alerts.some(item=>item.type==='RISK_THRESHOLD'));assert.ok(await watchlist.remove('u1',watch.id));});
