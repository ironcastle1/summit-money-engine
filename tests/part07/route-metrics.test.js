import test from 'node:test';import assert from 'node:assert/strict';import { calculateRouteMetrics } from '../../src/logistics/route-metrics.js';
test('route metrics calculate ETA, exposure, reliability, cost and emissions',()=>{const edge={id:'r1',name:'Test',from:'a',to:'b',mode:'SEA',distanceKm:3000,baseSpeedKmh:32,importance:80,coordinates:[[0,0],[20,10]],restrictions:{},metadata:{}};const path={cost:1,nodeIds:['a','b'],edges:[edge]};const request={vesselClass:'PANAMAX',cargoClass:'CONTAINERS',cargoTonnes:20000,departureAt:'2026-08-04T12:00:00Z'};const result=calculateRouteMetrics(path,request,{events:[{id:'e1',title:'Conflict',category:'conflict',lat:5,lon:10,severity:80,time:'2026-08-04T10:00:00Z'}]});assert.equal(result.segments.length,1);assert.ok(result.eta.durationHours>80);assert.ok(result.cost.totalUsd>0);assert.ok(result.exposure.eventCount>=1);assert.ok(result.reliability.score>0);});
