MERLIN CNC V5 — VERIFICATION REPORT
Generated: 2026-08-31

Architecture
- Deterministic CNC business operating system.
- No chatbot, LLM, OpenAI API, Ollama, model download, or token billing dependency.
- Tell MERLIN is a structured parse -> review -> commit intake console.
- Market intelligence is collected and summarized from public evidence by deterministic software.

Verification completed
- JavaScript syntax: PASS (37 .js files checked with node --check).
- Project preflight: PASS (scripts/preflight.js; 33 application JavaScript files checked).
- Relative import audit: PASS (0 missing local imports).
- Deterministic intake parser checks: PASS.
  - 5 sheets of 2mm steel at £110 => qty 5, £22/sheet.
  - 50 standoffs at £22.50 => qty 50, £0.45 each.
  - stocktake quantity parsing => PASS.
  - production qty/cut/cleanup time parsing => PASS.
  - unsupported vague statement is not committed => PASS.
- ID/SHA tests: PASS (2/2 using built-in Node test runner).
- SQLite schema static validation: PASS (30 tables).
- AI/model dependency audit in executable source/config: PASS.
- Runtime/user-data cleanliness: PASS (only .gitkeep files distributed under data/).

Dependency-backed integration test limitation
- A full npm install could not complete inside the artifact-generation environment before its network timeout.
- Therefore tests requiring better-sqlite3 and dxf-parser were not executed in this environment.
- GitHub CI remains configured to install dependencies and run npm check / npm test after push.
- Node is pinned to 20.19.5 for the deployment/runtime environment.

Important design limitation
- Arbitrary image -> production-ready DXF remains intentionally disabled. Generic image vectorization cannot yet be guaranteed to satisfy retained-steel topology, bridge widths, kerf, minimum-feature, and machine-specific plasma constraints.
